version: '3.9'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        GROQ_API_KEY: "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"
    ports:
      - "8000:8000"
    volumes:
    - ./logs:/app/logs
    working_dir: /app/src
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
